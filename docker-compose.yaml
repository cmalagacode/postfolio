services:
 web:
  build: .
  
  ports: [8080:8080]
  environment:
    - POSTFOLIO_ENV=${POSTFOLIO_ENV}
    - POSTFOLIO_DATABASE_URL=${POSTFOLIO_DATABASE_URL}
    - POSTFOLIO_SECRET_KEY=${POSTFOLIO_SECRET_KEY}